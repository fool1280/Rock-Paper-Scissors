import React from 'react'

export default function Start(props) {
    return (
        <div>
            <button onClick={props.ChoiceButton}> 
                
            </button>
        </div>
    )
}
